// script.js
window.addEventListener("load", function() {
    // Seleciona o elemento com a classe 'fade-in-box'
    var fadeInBox = document.querySelector(".fade-in-box");
    
    // Adiciona a classe 'show' após o carregamento da página
    fadeInBox.classList.add("show");
});
